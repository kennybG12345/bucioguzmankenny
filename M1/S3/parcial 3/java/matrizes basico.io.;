import java.io.*;
import java.util.Scanner;
class menu
{
    public static void main(String[] args){
        Scanner sn = new Scanner(System.in);
        boolean salir = false;
        int opcion;//Guardaremos la opcion del usuario
        do{
            System.out.println("1.opcion 1");
            System.out.println("2.opcion 2");
            System.out.println("3.opcion 3");
            System.out.println("4.salir");
            System.out.println("Escribe una de las opciones");
            opcion = sn.nextInt();
            switch (opcion)
            {
                case 1:
                    System.out.println("Has seleccionado opcion 1");
                    break;
                case 2:
                    System.out.println("Has seleccionado opcion 2");
                    break;
                case 3:
                    System.out.println("Has seleccionado opcion 3");
                    break;
                case 4:
                    salir = true;
                    break;
                default:
                    System.out.println("Solo numeros entre 1 y 4");
            }
        } while (opcion < 1 &&opcion > 3);


mport java.io.*;
class TablasMultiplicar
{
 public static void main(String[] args)
  {
   //crear matrices en java 
   
   //matrices de datos de tipo int llamado ventanas de 4 filas y 6 columnas:
   //int[]ventanas = new int [4][6];

   //matrizes de datos doubles llamado temperaturas de 3 filas y 4 columnas:
   //double [][]temperaturas = new double [3][4]

   //arrays irregulares
   //int[]][] m= new int [3][];
   
   //crear una matriz m de 3 filas
   //a cada fila sele puede asignar un numero distinto de columnas:

   //m[0]=new int [3]; 
   //m[1]=new int [5];
   //m[2]=new int [2];

   
  //Inicializar matrices

  //int[][] numeros={{6,7,5},{3,8,4},{1,0,2},{9,5,2}},
  
  //crear una matriz irregular de 4 filas.las primeras de 5 columnas,la segunda de 3,
  //la tercera de 4 y la cuarta de 2.
  //int[][] a ={{6,7,5,0,4},{3,8,4},{1,0,2,7},{9,5}};

  //Recorres matrices

  //para recorrer una matriz  se anidan dos bucles for en general para recorrrer una array multidimencional
  //se anidan tantas instrucciones for como dimenciones tenga el array

  //se introducen los valores de la matriz.
  //int A[][]={{5,1,0},{5,2,0},{5,3,0}};
  
  //Mostrar por pantalla los valores que contiene la matriz.
  //A.length indica el numero de filas de la matriz.
  //en este ejemplo el numero de (A.length)es 3.
  //A[f].length indica el numero de columnas de la fila f.En  este ejemplo .
  //la longitud de cada fila del array (A[f].length)es 3.
  System.out.println("el valores introducidos:");
  for(int F=0;f<A.length;f++
  {
   System.out.println(A[f][c]+"");
   }//for interno.
   System.out.println();
  }//For externo
 }//metodo main.
}//Clase tabla multiplicacion
